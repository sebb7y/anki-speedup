This folder is preserved when the add-on is upgraded. Place a custom alert
sound here (e.g. `alert.mp3`) and set `alertSound` in the add-on config to its
filename to use it.

Speedup also stores its optional local analytics database (`speedup.db`) here.
